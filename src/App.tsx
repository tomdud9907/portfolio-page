export default function App() {
  return (
    <main className="app">
      <section className="card">
        <p className="eyebrow">Portfolio setup</p>
        <h1>React + TypeScript + Vite is ready 🚀</h1>
        <p>
          Next step: install dependencies with <code>npm install</code> and run <code>npm run dev</code>.
        </p>
      </section>
    </main>
  )
}
